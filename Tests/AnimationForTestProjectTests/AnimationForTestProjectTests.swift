import XCTest
@testable import AnimationForTestProject

final class AnimationForTestProjectTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AnimationForTestProject().text, "Hello, World!")
    }
}
